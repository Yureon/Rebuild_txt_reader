# 개발 문서 인덱스 — v682

현재 기준은 `v682` / `6.82.0` / `rebuild-v682`이다. Owner 라이브러리 정리/재배치 스크립트는 Windows PowerShell 5.1 호환 상대경로 계산을 사용한다. 아래 canonical 문서와 실제 검증 결과를 현재 기준으로 사용한다.

<!-- v682-doc-index-pass -->

# 개발 문서 인덱스 — v681

현재 기준은 `v681` / `6.81.0` / `rebuild-v681`이다. Metadata 작품 목록의 적용 출처 필터에서 자동 Provider와 직접 입력(`manual`) 결과를 분리할 수 있다. 아래 canonical 문서와 실제 검증 결과를 현재 기준으로 사용한다.

<!-- v681-doc-index-pass -->

# 개발 문서 인덱스 — v680

현재 기준은 `v680` / `6.80.0` / `rebuild-v680`이다. 로그인 전 Owner 팔레트와 로그인 후 사용자별 scoped theme bootstrap이 현재 UI 계약에 추가됐다. 아래 canonical 문서와 실제 실행 결과를 기준으로 판단하며 과거 버전 섹션은 역사적 참고다.

<!-- v680-doc-index-pass -->

# 개발 문서 인덱스 — v679

현재 기준은 `v679` / `6.79.0` / `rebuild-v679`이다. 현재 계약은 아래 canonical 문서와 실행 결과를 기준으로 판단하며, v677 이하 기록은 역사적 참고다.

<!-- v679-doc-index-pass -->

# 개발 문서 인덱스

현재 기준은 `v676` / `6.76.0` / `rebuild-v676`이다.

| 영역 | 문서 |
|---|---|
| 이번 변경 | `release-notes.md` |
| 상태·로드맵 | `project-status-roadmap.md` |
| 인수인계 | `handoff.md`, `next-session-handoff-prompt.md` |
| 배포 | `deployment-guide.md`, `proxy-tunnel-setup.md` |
| 운영·진단 | `operations-checklist.md`, `production-diagnostics.md` |
| 보안·권한 | `security.md`, `multi-user-access-control.md`, `user-data-isolation.md` |
| 저장·성능 | `storage-architecture.md`, `normalized-content-cache.md`, `performance-cache.md` |
| Metadata | `web-metadata.md`, `library-metadata-grouping.md` |
| Reader | `reader-search-baseline.md`, `reader-anchoring-stability-contract.md` |
| PWA | `pwa-offline.md` |
| API | `api-contract.md` |
| 검증 | `smoke-tests.md`, `audit-resolution.md` |
| 과거 이력 | `release-history.md` |

버전별 단일 기능 문서를 누적하지 않는다. 현재 계약은 canonical 문서와 실제 코드·실행 결과를 기준으로 판단한다.

<!-- v676-doc-index-pass -->
