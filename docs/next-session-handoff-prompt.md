# 다음 세션 인수인계 프롬프트

TXT Reader Multi `v682` / package `6.82.0` / build `rebuild-v682`를 기준으로 유지보수한다. `txt_reader_v682.zip.sha256`과 실제 ZIP을 비교하고 archive safety, 새 빈 디렉터리 clean extract, `package-manifest-v682.json`의 path/bytes/SHA-256을 전수 검증한다.

Owner 라이브러리 정리/재배치 PowerShell 생성기는 Windows PowerShell 5.1 호환이어야 한다. `[System.IO.Path]::GetRelativePath()`를 다시 도입하지 말고, root containment·reparse-point·dry-run·Apply 계약을 보존한다. 실제 Windows PowerShell 5.1을 실행하지 않았다면 runtime 통과로 기록하지 않는다.

<!-- v682-next-session-handoff-pass -->

# 다음 세션 인수인계 프롬프트

TXT Reader Multi `v681` / package `6.81.0` / build `rebuild-v681`를 기준으로 유지보수한다. `txt_reader_v681.zip.sha256`과 실제 ZIP을 비교하고 archive safety, 새 빈 디렉터리 clean extract, `package-manifest-v681.json`의 path/bytes/SHA-256을 전수 검증한다.

Metadata 관리의 `수동 입력 (직접 추가)` 출처 필터와 `providerId: manual` 계약을 보존한다. 실제 Provider 필터, 상태·폴더·검색·cursor와 복합 적용할 때 선택·로드 위치가 초기화되지 않아야 한다. 기존 Owner 로그인 팔레트, 사용자별 theme, Reader `fileChar`, Metadata hard cap, library cursor, 사용자 격리와 PWA executable fail-closed도 유지한다.

실제 N100·80k HDD/SMB·Docker/Podman·Tunnel/NPM·live provider·제품 브라우저를 실행하지 않았다면 통과로 기록하지 않는다.

<!-- v681-next-session-handoff-pass -->

# 다음 세션 인수인계 프롬프트

TXT Reader Multi `v680` / package `6.80.0` / build `rebuild-v680`를 기준으로 유지보수한다. `txt_reader_v680.zip.sha256`과 실제 ZIP을 비교하고 archive safety, 새 빈 디렉터리 clean extract, `package-manifest-v680.json`의 path/bytes/SHA-256을 전수 검증한다.

로그인 Owner 팔레트, 사용자별 active theme scope, login theme prime, Metadata session theme bootstrap과 html/body/theme-color 동기화를 보존한다. 로그인 페이지에 `theme-boot.js`를 다시 넣어 이전 사용자 테마가 노출되게 하지 않는다. 기존 Reader `fileChar`, Metadata hard cap, library cursor, 사용자 데이터 격리와 PWA executable fail-closed도 유지한다.

실제 N100·80k HDD/SMB·Docker/Podman·Tunnel/NPM·live provider·Whale/Samsung Internet·arm64를 실행하지 않았다면 통과로 기록하지 않는다.

<!-- v680-next-session-handoff-pass -->

# 다음 세션 인수인계 프롬프트

TXT Reader Multi `v679` / package `6.79.0` / build `rebuild-v679`를 기준으로 유지보수한다. `txt_reader_v679.zip.sha256`과 실제 ZIP을 비교하고 archive safety, 새 빈 디렉터리 clean extract, `package-manifest-v679.json`의 path/bytes/SHA-256을 전수 검증한다.

v679의 Metadata folder query theme ownership, v679 cachebuster/Service Worker namespace, Metadata Helper 380px fixed popup width를 보존한다. v677의 Metadata hard resident cap·incremental heap eviction·tree cursor paging·cover/audit no-follow handle, Reader `fileChar`, 사용자 데이터 격리와 PWA executable fail-closed도 유지한다.

실제 N100·80k HDD/SMB·Docker/Podman·Tunnel/NPM·live provider·Whale/Samsung Internet·arm64를 실행하지 않았다면 통과로 기록하지 않는다.

<!-- v679-next-session-handoff-pass -->

