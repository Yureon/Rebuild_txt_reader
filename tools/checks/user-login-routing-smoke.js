#!/usr/bin/env node
const fs=require('fs'),path=require('path'),assert=require('assert');function r(x){return fs.readFileSync(path.join(__dirname,'../..',x),'utf8')}function runUserLoginRoutingSmoke(){const a=r('server/routes/auth-routes.js'),app=r('server/app.js'),admin=r('server/routes/admin-users-routes.js'),page=r('public/admin/users.html');assert.ok(a.includes('accountService.authenticateUser'));assert.ok(a.includes("redirectTo = '/site.html'"));assert.ok(a.includes('USER_SESSION_KIND'));assert.ok(app.includes('createAccountService')&&app.includes('paths.ACCOUNTS_PATH'));assert.ok(admin.includes("router.post('/admin/users'"));assert.ok(page.includes('OWNER CONSOLE · v417 skeleton UI'));return{pass:'v389-user-login-routing-smoke-pass'}}if(require.main===module)console.log(JSON.stringify(runUserLoginRoutingSmoke()));module.exports={runUserLoginRoutingSmoke};
