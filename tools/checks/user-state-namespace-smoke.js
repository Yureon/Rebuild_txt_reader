#!/usr/bin/env node
const fs=require('fs'),os=require('os'),path=require('path'),assert=require('assert');const {TXT_READER_MULTI_USER_STATE_NAMESPACE_PASS,createUserStateServiceManager,getStateOwnerId}=require('../../server/services/user-state-service');function runUserStateNamespaceSmoke(){const dir=fs.mkdtempSync(path.join(os.tmpdir(),'txt-reader-user-state-')),m=createUserStateServiceManager({userDataDir:dir,logger:{error(){}}}),a={kind:'user',userId:'reader-a'},b={kind:'user',userId:'reader-b'},o={kind:'owner',userId:'__owner__'};assert.strictEqual(getStateOwnerId(a),'reader-a');assert.strictEqual(getStateOwnerId(o),'__owner__');const A=m.getStatePathForSession(a),B=m.getStatePathForSession(b),O=m.getStatePathForSession(o);assert.notStrictEqual(A,B);assert.ok(A.endsWith(path.join('reader-a','state.json')));assert.ok(O.endsWith(path.join('__owner__','state.json')));fs.rmSync(dir,{recursive:true,force:true});return{pass:TXT_READER_MULTI_USER_STATE_NAMESPACE_PASS}}if(require.main===module)console.log(JSON.stringify(runUserStateNamespaceSmoke()));module.exports={runUserStateNamespaceSmoke};
