#!/usr/bin/env node
const fs=require('fs'),path=require('path'),assert=require('assert');function runMultiUserDesignDocSmoke(){const d=fs.readFileSync(path.join(__dirname,'../..','docs/multi-user-access-control.md'),'utf8');for(const t of ['owner 콘솔 전용 계정','migration하지 않는다','`folders` mode는 선택한 폴더와 모든 하위 폴더를 포함한다','/api/novels','/api/content','/api/search','/api/block-manifest','segment 단위 prefix 비교','v389-txt-reader-multi-user-account-pass'])assert.ok(d.includes(t),t);return{pass:'v389-txt-reader-multi-design-doc-smoke-pass'}}if(require.main===module)console.log(JSON.stringify(runMultiUserDesignDocSmoke()));module.exports={runMultiUserDesignDocSmoke};
